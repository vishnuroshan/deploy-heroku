import { TypeformatPipe } from './typeformat.pipe';

describe('TypeformatPipe', () => {
  it('create an instance', () => {
    const pipe = new TypeformatPipe();
    expect(pipe).toBeTruthy();
  });
});
